services:
  ai-sales-coach:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: khc-ai-sales-coach
    restart: unless-stopped
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./:/app
